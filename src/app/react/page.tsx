import Tree from "@/components/Tree";

// Name doesn't matter, but it is a good practice to name the file after the component
export default function ReactPage() {
    return(
        <>
            <Tree category="react"></Tree>
        </>
    );
}